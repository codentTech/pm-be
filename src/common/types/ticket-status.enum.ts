export enum TicketStatus {
  BACKLOG = 'backlog',
  READY = 'ready',
  IN_PROGRESS = 'in_progress',
  BLOCKED = 'blocked',
  CODE_REVIEW = 'code_review',
  QA = 'qa',
  DONE = 'done',
  REOPENED = 'reopened',
}
